# CS166 Study Guide

A simple web app for reviewing CS166 security concepts.

## Run locally

1. Install dependencies: `npm install`
2. Start the dev server: `npm run dev`
3. Open http://localhost:5173 in your browser

Requires [Node.js](https://nodejs.org/) (e.g. 18+).
